<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mapper.BaseDictDao">
<!-- 开启二级缓存 -->
<cache/>
	<!-- public List<BaseDict> queryBaseDictList(String duct_id); -->
	<select id="queryBaseDictList" parameterType="String" resultType="BaseDict">
		select * from base_dict where dict_type_code=#{value}
	</select>
</mapper>